@{
    ViewBag.Title = "Add";
}
@using (Html.BeginForm("Add", "User", FormMethod.Post, new { @id = "user-add" }))
{
    @Html.ValidationSummary(true)
    <div id ="user-editor-outer">
    @{  //commented out for now, don't think we need it now that main menu exists
    //<div>@Html.ActionLink("Back to List", "Index", null, new { id = "Back" })</div>
    }

    <div class="header ui-widget-header ui-corner-top">Add User</div>
    <div id="user-editor" class="ui-corner-bottom ui-widget-content ui-widget">
        <table id="user-editor-table">
            <tr>
                <td> User Name <div class="req">*</div> </td>
                <td>
                    <div>
                        <input id="UserName" class="text-box single-line host-standard-textbox required" type="text" value="" name="UserName"/>
                    </div>
                </td>
            </tr>
            <tr>
                <td> Password <div class="req">*</div> </td>
                <td>
                    <div>
                        <input id="Password" class="text-box single-line host-standard-textbox required" type="text" value="" name="Password"/>
                    </div>
                </td>
            </tr>
            <tr>
                <td> Role </td>
                <td>
                    <div>
                        @Html.DropDownList("role_id", (SelectList)ViewBag.RoleSelect)
                    </div>
                </td>
            </tr>
            <tr>
                <td>Active</td>
                <td>
                    <div id="ActiveButtonSet">
                        <input type="radio" id="Active1" name="Active" value="true" checked="checked"/><label for="Active1">True</label>
                        <input type="radio" id="Active2" name="Active" value="false" /><label for="Active2">False</label>
                    </div>
                </td>
            </tr>
        </table>
    </div>
    <input type="submit" id="btn-save" value="Save" />
</div>
}
@section Scripts {

<script>
    $(function () {
        $("input[id=btn-save]").button();
        $("#ActiveButtonSet").buttonset();
        $("#user-add").validate();
    });
</script>
}
